---
store:
  mode: READ_ONLY
server:
  externalUrl: ${EXTERNAL_URL:-https://demo.ldproxy.net}
backgroundTasks:
  maxThreads: 4
logging:
  level: OFF
  sqlQueries: OFF
  sqlResults: OFF
  stackTraces: ON
  appenders:
    - type: console
      timeZone: Europe/Berlin
  loggers:
    de.ii: INFO
